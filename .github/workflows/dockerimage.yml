name: Docker Compose Actions Workflow

on:
  push:
    branches: [13.x-docker]
  pull_request:
    branches: [13.x-docker]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Build the stack
        run: docker-compose up
